<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Password Reset</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
        }
        .email-container {
            max-width: 600px;
            margin: 20px auto;
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        .email-header {
            text-align: center;
            padding-bottom: 20px;
            border-bottom: 1px solid #ddd;
        }
        .email-header h1 {
            color: #333;
            font-size: 24px;
            margin: 0;
        }
        .email-body {
            padding: 20px 0;
        }
        .email-body p {
            color: #555;
            font-size: 16px;
            line-height: 1.6;
        }
        .email-footer {
            text-align: center;
            padding-top: 20px;
            border-top: 1px solid #ddd;
            font-size: 14px;
            color: #888;
        }
        .password {
            font-size: 18px;
            font-weight: bold;
            color: #7fad39;
            margin: 10px 0;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #7fad39;
            color: #ffffff;
            text-decoration: none;
            border-radius: 5px;
            margin-top: 20px;
        }
        .button:hover {
            background-color: #6a8f2f;
        }
    </style>
</head>
<body>
    <div class="email-container">
        <div class="email-header">
            <h1>Password Reset</h1>
        </div>
        <div class="email-body">
            <p>Hello,</p>
            <p>You have requested a password reset. Here is your new temporary password:</p>
            <p class="password">{{ newPassword }}</p>
            <p>Please log in using this password and change it immediately after logging in.</p>
        </div>
        <div class="email-footer">
            <p>If you did not request this password reset, please contact support immediately.</p>
        </div>
    </div>
</body>
</html>