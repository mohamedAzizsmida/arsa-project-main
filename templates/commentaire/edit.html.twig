{% extends 'baseMainUser.html.twig' %}

{% block body %}
    <h1>Modifier le Commentaire</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
         <button type="submit" class="btn btn-primary">Modifier</button>
    {{ form_end(form) }}

    <a href="{{ path('app_commentaire_back', {'id': commentaire.post.id}) }}" class="btn btn-secondary">Retour</a>

{% endblock %}
